spring:
    main:
        allow-bean-definition-overriding: true
    application:
        name: kotlin-service
    cloud:
        consul:
            config:
                fail-fast: ${FAIL_FAST:true}
                format: yaml
