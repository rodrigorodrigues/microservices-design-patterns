spring:
    application:
        name: authentication-service
    cloud:
        consul:
            config:
                fail-fast: ${FAIL_FAST:true}
                format: yaml
    main:
        allow-bean-definition-overriding: true
management:
    endpoints:
        web:
            exposure:
                include: '*'
