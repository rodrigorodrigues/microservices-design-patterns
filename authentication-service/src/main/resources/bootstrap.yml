spring:
    application:
        name: authentication-service
    main:
        allow-bean-definition-overriding: true
    mvc:
        pathmatch:
            matching-strategy: ant_path_matcher
    cloud:
        kubernetes:
            enabled: false
