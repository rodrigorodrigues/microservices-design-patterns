spring:
    config:
        import: "optional:consul:"
    main:
        allow-bean-definition-overriding: true
    cloud:
        discovery:
            enabled: false
        consul:
            enabled: false
            config:
                enabled: false
        kubernetes:
            enabled: false
    data:
        redis:
            repositories:
                enabled: false
    mongodb:
        embedded:
            version: "4.0.2"
    session:
        store-type: none
opentracing:
    jaeger:
        enabled: false
management:
    health:
        redis:
            enabled: false
