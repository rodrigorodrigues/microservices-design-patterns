spring:
    cloud:
        discovery:
            enabled: false
        consul:
            config:
                enabled: false
                watch:
                    enabled: false
            enabled: false
            discovery:
                enabled: false
                catalog-services-watch:
                    enabled: false
            retry:
                enabled: false
        kubernetes:
            enabled: false
            config:
                enabled: false
    main:
        allow-bean-definition-overriding: true
    mongodb:
        embedded:
            version: "4.0.2"
opentracing:
    jaeger:
        enabled: false
server:
    error:
        include-message: always
