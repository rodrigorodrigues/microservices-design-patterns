apiVersion: v1
kind: Service
metadata:
  name: week-menu-api
  annotations:
    cloud.google.com/app-protocols: '{"https-port":"HTTPS","http-port":"HTTP"}'
  labels:
    app: week-menu-api
spec:
  ports:
    - name: https-port
      port: 8443
    - name: http-port
      port: 3002
  selector:
    app: week-menu-api
  type: NodePort
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: week-menu-api
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: week-menu-api
        version: v1
    spec:
      containers:
        - name: week-menu-api
          image: eu.gcr.io/spring-boot-gke-243520/nodejs-week-menu-api:6.0
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              memory: "256Mi"
            limits:
              memory: "512Mi"
          volumeMounts:
            - name: spendingbetter-p12
              mountPath: "/etc/ssl_certs_p12"
              readOnly: true
            - name: spendingbetter-crt
              mountPath: "/etc/crt"
              readOnly: true
          env:
            - name: JAVA_OPTS
              value: -Xms256m -Xmx512m
            - name: SERVER_PORT
              value: "3002"
            - name: SPRING_PROFILES_ACTIVE
              value: google
            - name: MONGODB_URI
              value: mongodb://mongodb:27017/docker
            - name: REDIS_SERVER
              value: redis
            - name: EUREKA_PATH
              value: /eureka/eureka/apps/
            - name: EUREKA_SERVER
              value: eureka-server
            - name: HOST_NAME
              value: week-menu-api
            - name: IP_ADDRESS
              value: week-menu-api
            - name: EUREKA_INSTANCE_PREFER_IP_ADDRESS
              value: "true"
#            - name: KEYSTORE_PASSWORD
#              valueFrom:
#                secretKeyRef:
#                  name: secrets
#                  key: KEYSTORE_PASSWORD
#            - name: KEYSTORE_FILE
#              value: "/etc/ssl/certs/www.spendingbetter.com.p12"
          ports:
            - containerPort: 3002
          livenessProbe:
            httpGet:
              scheme: HTTP
              path: /actuator/health
              port: 3002
            initialDelaySeconds: 200
            periodSeconds: 5
      volumes:
        - name: spendingbetter-p12
          secret:
            secretName: spendingbetter-p12
        - name: spendingbetter-crt
          secret:
            secretName: spendingbetter-crt
