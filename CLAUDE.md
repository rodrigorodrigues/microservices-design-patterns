## Agent skills

### Issue tracker

Issues live in GitHub Issues for `rodrigorodrigues/microservices-design-patterns`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single root `CONTEXT.md` for now (system-wide vocabulary); expected to grow into a multi-context layout (`CONTEXT-MAP.md` + per-service `CONTEXT.md`) as services accrue local vocabulary. See `docs/agents/domain.md`.
