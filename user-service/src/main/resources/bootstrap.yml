spring:
    main:
        allow-bean-definition-overriding: true
    application:
        name: user-service
    cloud:
        consul:
            config:
                fail-fast: ${FAIL_FAST:true}
                format: yaml
