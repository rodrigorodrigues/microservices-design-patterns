server:
    port: ${PORT:8888}

spring:
    cloud:
        config:
            server:
                git:
                    uri: https://github.com/rodrigorodrigues/java8-springboot.git
                    clone-on-start: true
                    searchPaths: config-server
    application:
        name: config-server
