spring:
    application:
        name: edge-server
    cloud:
        config:
            discovery:
                enabled: true
                service-id: config-server
eureka:
    client:
        register-with-eureka: false
